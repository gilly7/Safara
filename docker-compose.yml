services:
  chroma:
    image: chromadb/chroma
    container_name: safara-chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma-data:
